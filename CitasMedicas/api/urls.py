# api/urls.py
from django.urls import path
from . import views

urlpatterns = [
    # Aquí agregas las rutas de tus API endpoints
 #   path('example/', views.example_view, name='example'),
    # Agrega más rutas según sea necesario
]
